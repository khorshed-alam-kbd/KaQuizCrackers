import React from 'react';

const QuizQuestion = (option) => {
    console.log(option)
    return (
        <div>
            {/* <p className='border rounded border-primary m-3 p-3' >{option}</p> */}
        </div>
    );
};

export default QuizQuestion;